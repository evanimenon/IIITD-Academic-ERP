# Copy this to db.properties and fill your own values.
# Do NOT commit db.properties to git.

# --- Auth DB (users_auth) ---
auth.jdbcUrl=jdbc:mysql://localhost:3306/auth_db
auth.username=auth_user
auth.password=secret

# --- ERP DB (students, courses, etc.) ---
erp.jdbcUrl=jdbc:mysql://localhost:3306/erp_db
erp.username=erp_user
erp.password=secret
